# Swing Trading — direct runtime dependencies (pinned 2026-07-08).
# Full transitive lock in requirements.lock (from `pip freeze`).
# Resolved on Python 3.14.4; pandas/numpy are bleeding-edge majors here
# (pandas 3.0.x has no cp313/earlier constraint issues but changes defaults) —
# if a pandas/numpy edge breaks M0.2/M2, revisit the Python version, don't
# code around it. See docs record Appendix E.
yfinance==1.5.1
httpx==0.28.1
# pytest optional — frozen-regression tests run via their own __main__
# (swing_bot/test_frozen.py), matching the Trading project's pattern.
